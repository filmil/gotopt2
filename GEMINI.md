# Instructions for Gemini

- Use **Conventional Commits** for making commit messages.
- Use `bazel build //...` to build the project.
- Use `bazel test //...` to run the tests.
- Read `README.md` for general instructions.
